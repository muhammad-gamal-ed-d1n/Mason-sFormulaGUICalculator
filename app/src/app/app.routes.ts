import { Routes } from '@angular/router';
import { GraphEditorComponent } from './components/graph-editor/graph-editor';

export const routes: Routes = [
  { path: '', component: GraphEditorComponent },
  
];